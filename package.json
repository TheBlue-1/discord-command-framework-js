{
  "name": "discord-command-framework-js",
  "version": "0.0.1",
  "description": "framework for discord.js",
  "main": "src/index.ts",
  "scripts": {
    "build": "rimraf ./dist && tsc && cp README.md dist/ && cp release.package.json dist/package.json",
    "lint": "eslint . --ext .ts",
    "type-check": "pnpx tsc -p ./tsconfig.src.json --noEmit",
    "lint": "pnpx eslint . --quiet true"
  },
  "dependencies": {
    "discord.js": "^13.1.0",
    "glob": "^7.2.0",
    "rimraf": "^3.0.2",
    "rxjs": "^7.3.0"
  },
  "devDependencies": {
    "@types/node": "^17.0.21",
    "@typescript-eslint/eslint-plugin": "^5.15.0",
    "@typescript-eslint/parser": "^5.15.0",
    "eslint": "^8.11.0",
    "ts-node": "^10.7.0",
    "typescript": "^5.2.2"
  }
}
